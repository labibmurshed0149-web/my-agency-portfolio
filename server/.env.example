PORT=4000
# Copy this file to .env and fill the values before running the server

# SMTP settings — for Gmail, enable 2-step verification and create an App Password
# Example for Gmail (secure port 465):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=axioralab335@gmail.com
SMTP_PASS=Axioralab2026

# Recipient for contact notifications (defaults to SMTP_USER)
CONTACT_TO=axioralab335@gmail.com

# Optional: FROM address used in outgoing emails (defaults to SMTP_USER)
# FROM=no-reply@yourdomain.com

